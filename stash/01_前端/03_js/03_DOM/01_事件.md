---
title: 01_事件
---
## onclick 事件

```js
// 获取按钮对象
var btn = document.getElementById("btn");

// 绑定一个单击事件
btn.onclick = function(){
 alert("点击");
}
```

需要注意的是, 如果事件绑定在 html 加载前(`btn` 对象创建前)的话, 由于没有`btn`对象, 故事件无法被绑定.

## onload 事件

- onload 事件会在整个页面加载完成之后才触发

- 为 window 绑定一个 onload 事件.这样可以确保我们的代码执行时所有的 DOM 对象已经加载完毕了
- 追求性能的话应该放在后面

```js
window.onload = function(){
 // 获取按钮对象
  var btn = document.getElementById("btn");

  // 绑定一个单击事件
  btn.onclick = function(){
    alert("点击");
  }
};
```
